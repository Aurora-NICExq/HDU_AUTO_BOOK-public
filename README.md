![logo](./docs/logo.png)
# HDU Auto Book 杭州电子科技大学图书馆自动预约脚本

*<center><font size=2 font-style="italic"> “当知识织就的茧房温柔剥落，你在图书馆沉淀的岁月自会羽化成风，托起你向青空而去的锋利轨迹。” </font> </center>*

> [!IMPORTANT]
> 请遵守以下使用协议,若不同意此协议，请移步其它项目
> <details> <summary> 使用协议 </summary>
>
>  - 请合理使用此脚本，切勿占用公共资源，做出诸如预约但不去签到使用的行为。
>  - 本项目仅供学术交流使用，作者不对任何因使用本脚本造成的后果负责,包括但不仅限于由滥用脚本导致的封号，账号被锁定等。
>  - 本项目将停止维护并将被移除，当发生以下情况之一:
>    - 本项目被杭电图书馆或校方要求删除
>    - 作者发现本项目影响到了杭电图书馆正常的预约服务
>    - 作者发现本项目被滥用或有其他不妥之处
> - 当本项目被移除后，请各位使用者自觉停止使用fork的代码，以免造成不必要的麻烦。
> </details>

> [!NOTE]
> `2026.9` 适配现图书馆系统：登录改为学校统一身份认证平台（CAS / 信息门户账号）。定时任务预约**后天** 7:00–22:00。七天的 `启用` 默认均为 `false`，需自行打开要约的那天。

> [!NOTE]
> 本脚本于`2025.4.4`进行了最后一次更新（新增自习室楼层），但本项目应该可以在很长一段时间内可以正常工作，大家可以自行配置下试试是否可用，若有人愿意接手本项目，可以邮件联系我。

- [配置](#配置)
- [设置预约时间](#设置预约时间)
- [启用脚本](#启用脚本)

## 它做什么

每天 **19:57**、**20:57**（上海时区）自动跑：

1. 若后天对应星期的 `启用` 为 `false`，立即结束，不登录
2. 用学校统一身份认证（CAS）登录图书馆
3. 已有预约则结束，不再提交
4. 否则提交一单：后天 7:00–22:00；`name` 为区域名时在该区域随机抽座，为 `自定义` 时用文件底部的座位号
5. 座位被占会换一个再试；接口返回已有预约则停止

20:57 是补跑。不要反复手动点运行。

## 视频教学

[https://github.com/HaleyCH/HDU_AUTO_BOOK-public/blob/V2/docs/tutorial.mp4](https://github.com/user-attachments/assets/03444438-6150-4b71-8f39-7d5f4eda3992)

> 视频录制于旧版系统，登录界面现已变更为学校统一身份认证平台，但 Secrets 配置与 `user_config.yml` 的设置方法仍然适用。

## 配置
 1. fork本仓库
 2. 点击仓库中的 `setting` 标签， 选中 `Secrets` 选项卡（Settings → Secrets and variables → Actions）
 3. 点击 `New repository secret` 按钮，新建环境变量。

   | 环境变量名| 说明        | 用途                                              |
   |:----------|:------------------------------------------------|:--------------|
   | `SCHOOL_ID` | 你的学号      | 用于登录[杭电智慧图书馆](https://hdu.huitu.zhishulib.com/)（经学校统一身份认证平台跳转） |
   | `PASSWORD` | 学校统一身份认证密码（即信息门户密码） | 用于登录学校统一身份认证平台 |
   | `SCKEY`（选填） | 微信推送服务    | 详见 [Sever酱](https://sct.ftqq.com/) 配置微信推送打卡结果   |

![示范](docs/img1.png)

本地运行时：

```bash
export SCHOOL_ID='学号'
export PASSWORD='信息门户密码'
python main.py
```

## 设置预约时间
 1. 点开 `user_config.yml` 文件
 2. 修改其中日期所对应的设置。默认七天均为 `启用: false`，要约哪天把那天改为 `true`。
 3. 区域名、固定座位号和座位 ID 范围（含端点，不连续区间分行）写在 `user_config.yml` 注释里，指定座位只需改这个文件。

 | 名称      | 值                                | 功能        | 示范                       |
|---------|----------------------------------|-----------|--------------------------|
| `启用`    | `true`/`false`                   | 是否预约「后天」落到这一天 | 启用: true                 |
| `name`  | `二楼西`/`二楼东`/`十二楼`/`自定义` 等     | 预定座位位置    | name: 二楼西               |
| `开始时间`  | 24小时对应数字                         | 使用座位开始时间  | 开始时间: 7 (代表 7:00 开始) |
| `持续小时数` | 数字                               | 使用座位小时数   | 持续小时数: 15（即 7:00–22:00） |
| `自定义`   | yaml 列表，内容为座位号（需将 `name` 改为 `自定义`） | 使用自定义座位号  | 自定义：<br>  - 60710         |

 4. 保存文件并点击 `Commit changes` 按钮

![示范](docs/img2.png)

## 启用脚本
 1. 点击仓库中的 `Actions` 标签
 2. 点击 `I understand my workflows, go ahead and enable them` 按钮

![示范](docs/img3.png)

### 完成后它将于每天晚上 19:57 与 20:57 自动运行，预约**后天**的座位。你也可以在 `Actions` 标签中手动运行（同样会先检查 `启用` 和已有预约）。

### ❗Notice：由于GitHub不能保证cron完全准时，故该项目不适合用作抢座脚本。

# 致谢
- [杭州电子科技大学健康打卡脚本](https://github.com/YeQiuO/HDU_AUTO_PUNCH)

# 写在最后
这个简单的脚本伴我度过了在图书馆的日日夜夜，而如今终于抵达告别的码头。本来只是为了我能抢到座位而写的脚本，没想到在这几年间也多多少少帮了一些同学的忙，对此我深表荣幸。如今我即将离开校园，无法再更新本项目，对此本人感到非常抱歉。也在此祝愿各位热爱在图书馆学习的同学，*当知识织就的茧房温柔剥落，你在图书馆沉淀的岁月自会羽化成风，托起你向青空而去的锋利轨迹。*
